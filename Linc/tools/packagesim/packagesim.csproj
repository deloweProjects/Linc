<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>linc-packagesim</AssemblyName>
    <RootNamespace>Linc.PackageSim</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!--
      M12c: compiled verbatim from Linc.Desktop, same posture as apkinstallsim/ApkInstall.cs
      (dependency-free — no WinUI, no AdvancedSharpAdbClient) so check 7 below calls the real
      ToolLocator.FindAdb() production logic instead of modelling its candidate list.
    -->
    <Compile Include="..\..\DESKTOP\Linc.Desktop\Services\ToolLocator.cs" />
  </ItemGroup>
</Project>
