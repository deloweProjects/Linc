# TASK M12d — the owner renamed `SCRCPY/Custom` → `SCRCPY/Linc.scrcpy`; make the code follow

> **Agent: Claude Code 2.** Read `Vibe/agent/claude-code-2/GUIDE.md` first.
>
> **ONE PART, ONE SESSION.** This is a rename-follow, not a redesign. It should be boring. If it stops
> being boring, stop and report.

---

## 0. FIRST TWO STEPS

**0.1 — Inspect what is already on disk and report it.**
**0.2 — Build immediately, before writing anything.** **Then `git clean` nothing and delete nothing** —
instead, note that a *stale* build output may still contain the old `SCRCPY/Custom/` copy and make
things look healthier than they are. **Delete `bin/` and `obj/` for `Linc.Desktop` before your baseline
publish** so you are measuring reality, not leftovers.

---

## 1. Standing rules

- Workspace **yellow**; code **Linc** (`Linc`).
- **Code + tests only. Do not edit any `.md` file** — the planner updates docs. **Report every `.md`
  that mentions the old path.**
- **NO PROTOCOL WORK. No behaviour changes.** Same files shipped, same lookup order, same candidate
  count, same directory depth. **Only the folder name differs.**
- **Never move the real cursor. Never tap the phone. Do not install the APK.**
- Desktop build: `Stop-Process` any `Linc.Desktop`, then
  `& "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\amd64\MSBuild.exe" DESKTOP\Linc.Desktop.sln -p:Configuration=Debug -p:Platform=x64 /v:m`
- Publish: `-t:Publish -p:PublishProfile=Beta -p:Configuration=Release -p:Platform=x64`
- Harness CWD = `Linc`.

---

## 2. What happened, and the current broken state

**The owner renamed the folder by hand.** On disk it is now `SCRCPY/Linc.scrcpy/` containing `bin/`,
`src/` and `linc.ico`, sitting beside the upstream submodule `SCRCPY/Default/`.

**No code followed the rename.** Planner-verified right now:

- `Services/ToolLocator.cs` — **nine** `SCRCPY/Custom` references across `FindAdb`, `FindScrcpy` and
  `FindScrcpyIcon` (both bundled and dev-tree candidates in each).
- `DESKTOP/Linc.Desktop/Linc.Desktop.csproj` line 28 — `<Content Include="..\..\SCRCPY\Custom\bin\**\*">`
  with a matching `<Link>` on line 29. **This glob now matches zero files**, so a fresh publish ships
  **no scrcpy and no adb at all.**
- `tools/packagesim/Program.cs` — **20** references.
- Possibly `.gitignore`, `Beta.pubxml`, and other harnesses. **Find them all; the counts above are what
  I found, not a guarantee of completeness.**

**So the tree currently builds clean and produces a broken package.** That is exactly the failure mode
the last three sessions existed to eliminate, which is why the acceptance below is a publish-output diff
rather than a green build.

**The name is the owner's choice and is final: `Linc.scrcpy`.** Do not "improve" it, do not shorten it,
do not move the folder.

---

## 3. The work

**3.1 Find every reference first and list them before changing any.** Grep the whole tree for `Custom`
in `.cs`, `.csproj`, `.pubxml`, `.gitignore`, `.gitmodules`, `.ps1`, harnesses and scripts. **Beware
false positives** — `Custom` appears in unrelated contexts (a "Custom" scrcpy quality preset in
`MirrorSettings`, `DesktopModeSettings`' `Custom` geometry mode). **Change only the ones that are the
scrcpy bundle path.** Report the ones you deliberately left alone.

**3.2 Update the paths.** `SCRCPY/Custom` → `SCRCPY/Linc.scrcpy` everywhere it means the bundle.

**3.3 Do not change lookup order, candidate counts, or the six-level dev-tree depth.** M12c fixed that
depth after finding it wrong since M3 — **preserve it exactly.**

**3.4 Leave `SCRCPY/Default` alone.** It is a git submodule.

**3.5 Update the harnesses so they keep testing the real thing.** `packagesim`'s fixture layout must
mirror the new name. **A harness that passes because it now checks a path that no longer exists is the
failure mode here** — each one must still fail if the bundle were genuinely missing (proved in §4.5).

**3.6 Check the `src/x/` build-output gitignore rule** still matches under the new folder name, so
scrcpy's own build artefacts do not start showing up in `git status`.

---

## 4. Acceptance

1. MSBuild x64 Debug → 0 errors, no new warnings (`CS9113` on `HomeViewModel.cs:166` is pre-existing).
2. **Delete `bin/`/`obj/`, publish fresh, and list the output.** `publish/SCRCPY/Linc.scrcpy/bin/` must
   contain the **full 104 files** including `adb.exe`, `scrcpy.exe` and `scrcpy-server`. **Paste the
   count and the three filenames.** A publish that ships an empty or missing bundle is the whole risk.
3. `packagesim` green, exit 0 — including the real-`ToolLocator.FindAdb()`-against-a-fixture check,
   whose fixture must use the new name.
4. Full regression: `presencesim`, `startupsim`, `applaunchsim`, `mirrorsettingssim`, `appssim`,
   `homelayoutsim`, `displaysim`, `homecachesim`, `storesim`, `synccachesim`, `outboxsim`,
   `apkinstallsim` all green. `desktopsim`'s ADB section honest either way.
5. **Two negative proofs against PRODUCTION files:**
   - Point `ToolLocator`'s bundled adb candidate back at `SCRCPY/Custom/...` → `packagesim`'s
     real-function check fails → restore → green. *(Proves the harness follows the rename rather than
     passing on a stale assumption.)*
   - Break the csproj `Content` glob back to `SCRCPY/Custom/bin/**/*`, re-publish → the payload check
     fails because the bundle is missing → restore, re-publish → green. *(This is the exact bug the
     rename introduced; prove the harness now catches it.)*
6. **`git status` from `Linc/`.** The owner renamed by hand, so git likely sees a large delete-plus-add
   rather than a move. **Report what it looks like; do not attempt to rewrite history or re-stage it.**

---

## 5. Report

- §0.1 findings and the §0.2 baseline.
- **Every reference you changed, file by file**, and **every `Custom` you deliberately left alone**
  (the quality preset, the geometry mode, anything else) — I want to see that you distinguished them.
- **Every `.md` that mentions the old path** — do not edit; list for the planner.
- The publish output listing (§4.2), with the file count.
- Files changed, one line each. Verbatim build/publish/harness output. Both negative proofs, naming the
  file broken. `git status`.
- **A one-line manual test for the owner:** re-copy `publish/` to the second PC, start a mirror, confirm
  scrcpy launches and Settings shows an adb path under `SCRCPY/Linc.scrcpy/bin/`.
- **What you could NOT verify and why.**
- Anything in this file that was ambiguous, contradictory or wrong.

## 6. Out of scope

Moving the folder anywhere; touching `SCRCPY/Default`; changing lookup order, candidate count or depth;
editing `.md` files; rebuilding scrcpy from source; the tray-icon `ToSmallIcon` exception; M4; M13;
committing anything.
