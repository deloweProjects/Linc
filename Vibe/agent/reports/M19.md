# REPORT M19 — restructure for GitHub, repo-embedded update channel, dynamic colour

**Status: all five parts complete. Committed locally, NOT pushed.** No remote is configured.

## 0. First steps
**0.1** Branch `main`, HEAD `b761eaf`, `d64a1b7`, `01d7504`; 57 status entries (uncommitted M17b/M18
work). **0.2 Baselines** — Desktop **0 errors, 1 unique warning** (CS9113 ×2); Android **155 tests,
0 failures**; `PROTOCOL_VERSION = 18`, `Envelope.Version = 18`.

### 0.3 — THE REMOTE, verbatim. **Safe. Nothing there is not ours.**

```
$ git ls-remote https://github.com/deloweProjects/Linc
29aefbb3b0ec53ecbd9381f458fe2c144e7442cd	HEAD
29aefbb3b0ec53ecbd9381f458fe2c144e7442cd	refs/heads/main
```
Cloned to a scratch dir. **One commit**, `29aefbb "Initial commit"` by `delowe`, 2026-07-20. **One
file**: `README.md`, entire content `# Linc`. No other branches, no tags, no LFS. I continued.

> ⚠️ **BEFORE YOU PUSH:** our `main` (parent `b761eaf`) and the remote's `29aefbb` are **unrelated
> histories** — a plain `git push` will be rejected. The remote holds only that one-line README so
> overwriting loses nothing, but **it is your call**: `git push --force origin main`, or
> `git pull --allow-unrelated-histories` first. Add the remote yourself; I did not.

## PART A — layout ✅

**A1 `Files/` → `Linc/Documentation/`.** On disk the rename is **`DOCUMENTATION`** (all caps); the task
spells it `Documentation/` three times, so I used the task's spelling. **25 files** rewritten;
`C:\Program Files\` and `Generated Files\` excluded by guard and intact. My first sweep also hit 7
**historical** files (`reports/M15a,M15b,M4c`, `tasks/M16,M18,M4c,M19`) — transcripts of what was true
at the time — so I **reverted those** and restored `M19.md` verbatim (a task file is immutable).

**A2** `prod/` (200 MB) + `prod.rar` (170 MB) moved to **`D:\deloweProjects\Linc-prod\`** — outside the
tree, nothing deleted. `Releases/` = `CHECKSUMS.txt` (copied), `RELEASE-NOTES.md` (written; says
plainly the binaries are GitHub Release assets), `update.json`.

**A3** `ico.png` → `Assets/`. **No code reads it** — the only references were docs and M17a's script,
which lives inside a task file, so there was nothing to repoint.

**A4** One merged root `.gitignore`. **`MCP/` is excluded — I asked nothing and just did it.** **I
caught a real bug in my own first draft:** `**/bin/` swallowed the **104 vendored scrcpy binaries**
(scrcpy.exe, adb.exe, SDL2, the mingw64 DLLs) the desktop cannot mirror without. Added
`!Linc/SCRCPY/Linc.scrcpy/bin/` + `/**` after the rule; all 104 verified staged. **`git status
--short | wc -l` after the rules: 766** (757 old paths deleted + 5 collapsed dirs) — not thousands.

### A5 — SECRETS SWEEP. This gates publication. Every hit and its disposition:

| Pattern | Hits | Disposition |
|---|---|---|
| Phone serial `<your-device-serial>` (+1 lowercase twin) | **46 in 27 files** | **Replaced** with `<your-device-serial>` |
| `C:\Users\<you>\` | **5** | **Replaced** with `C:\Users\<you>\` |
| `D:\deloweProjects\yellow\` (+1 ellipsis form) | **173 in 70 files** | **Replaced** repo-relative (Part B) |
| `nvapi-`, `ghp_`/PAT, `AKIA`, emails | **0 outside `MCP/`** | — |
| `sk-`, `api_key=`, PRIVATE KEY | 6, **all in `MCP/.venv/`** | `MCP/` **gitignored** — verified |
| `.env`, `.claude/`, `.continue/`, `skills-lock.json` | at root | All **gitignored** — verified |
| IPv4 private/loopback (192.168.x, 127.0.0.1, 10.x) | 32 distinct | Test fixtures; not secrets, left as-is |
| IPv4, **your own adapters** — Radmin VPN address/gateway, CGNAT address | 19 | **Redacted in M19-amend** on the owner's ruling. Docs now read `<vpn-address>` / `<vpn-gateway>` / `<cgnat-address>`; the `hotspotsim` fixture uses RFC 5737 documentation addresses. |

Residual scan after redaction: **zero** serial and username hits outside `MCP/`.

> **Regression I introduced and fixed:** redacting the serial broke `hotspotsim`'s case-insensitive
> identity assertion — only the uppercase form was replaced, leaving a placeholder compared against a
> real lowercase serial. Fixed by announcing `<YOUR-DEVICE-SERIAL>`, which still differs in case. Green.
> **Behaviour change:** `tools/probe` hardcoded your phone's serial as its fallback and now falls back
> to the placeholder, so **set `LINC_SERIAL`** before running it.

## PART B — `Vibe/` ✅

**B1** `master-docs/` → `Vibe/master/`, `agent-docs/` → `Vibe/agent/`. Nothing dropped, including the
dead `gemini-docs/`/`claude-docs/` (GUARDRAILS forbids deleting — **flagging** that shipping two stale
doc sets publicly may be worth revisiting).

**B2 — the path rewrite. 131 doc files changed in the commit**, over three passes: absolute →
repo-relative (108 files / 304 lines), folder renames + separator normalisation (102 / 1214), mixed
separators the ordering left behind (73 / 292). **Windows command lines left literal** — I only
converted spans that are bare paths with no drive letter. Three spot-checks:

- `Vibe/agent/AGENTS.md:7` — `D:\deloweProjects\yellow\` → `` `<repo-root>/` ``
- `Vibe/agent/tasks/M12.md:3,27,33` — `` `Vibe/agent/claude-code-2/GUIDE.md` `` and
  `` `Vibe/agent/opencode-docs/` `` rewritten; the MSBuild line on 33 **untouched**.
- `Vibe/master/ORCHESTRATOR.md:34` — `Linc\Files\` → `` `Linc/Documentation/` ``.

**B3** `Vibe/START-HERE.md` (132 lines): what Linc is, the two roles with read-orders, the loop, the
five standing rules, the five-minute list, why the ledger is kept. **No agent-side resume prompt
existed**, so I wrote **`Vibe/agent/RESUME.md`**; START-HERE points at both. **B4** ledger kept in full.

## PART C — the update channel ✅

**C1** `Releases/update.json` is the channel, read from the raw URL baked in as a constant —
`UpdateChannel.ManifestUrl` (new) and `UpdateSettings.MANIFEST_URL`, each commented that changing one
means changing the other. Shape documented in the file itself **and** in `RELEASE-NOTES.md`,
including that raising `minimumSupported` is what forces an update.
**C2** `UpdateDecision` (both languages) is **byte-unchanged** — only the manifest's source moved.
`UpdateManifest` gained the nested `desktop`/`android` assets, keeping M17b's flat `url`/`sha256` as a
fallback. **No protocol work; both constants still 18.**
**C3** The manifest-URL text box is gone from **both** Settings pages. What remains: one switch **"Keep
Linc up to date", default ON**, a **"Check now"** button, the M17b optional card / forced screen
untouched, and an off-state that says where to update by hand. Registry: `UpdateManifestUrl` retired,
`UpdatesEnabled` **appended at the end of the positional `Settings` record with the retired slot kept
and written null**, so an M17b-era `settings.json` still round-trips.
**C4** Unreachable/malformed/garbage → `None`, logged at Info (no Debug level exists here — same call
M17b made), never surfaced; hash mismatch refuses in plain language. New harness check [6/7].
**C5 proved:** `updatesim` **[7/7] reads the committed `Releases/update.json` off disk** and gets
`None` for both `1.0.0-beta.1` and the numeric `1.0.0.0` assembly version.

## PART D — the public face ✅

**D1 `README.md`** (126 lines): one-line pitch, a **why** section (no servers, no account, no backend
— the repo *is* the infrastructure), an ASCII diagram of the two ranked links, the feature list, **an
honest Status section sourced from `reports/M18.md`** (debug-signed APK, Debug WinUI, hand-verified
mirror input, stale `Documentation/`, no CI), install, requirements, `@delow3`, MIT. **Badges: licence,
status, platform only — nothing claiming CI or coverage.** **D2 `CONTRIBUTING.md`**: both build
commands with the MSBuild-x64 and kill-the-tray-exe traps, harness rules (CWD = `Linc/`, the
`devicesim` footgun), existing conventions, **protocol-is-sacred as its own section**, and an up-front
note that the project is largely AI-built with `Vibe/` explaining the workflow. **D3 `LICENSE`** MIT,
`(c) 2026 delow3`. **D4** two issue templates. **No CI workflow.**

## PART E — Android dynamic colour ✅

**E1** `Theme.kt` restores `dynamicLightColorScheme`/`dynamicDarkColorScheme` on **API 31+**, falling back
to the Linc neutral ramp on API 30 and on any platform failure (try/catch — a device with no wallpaper
colours must not crash). Desktop `UsePhoneColours` untouched.

> **Task-file wording slip:** E1 says "Android 12+" *and* "fallback on API 30-31". Android 12 **is**
> API 31, so those contradict. I implemented dynamic at **API 31+**, neutral at API 30 (`minSdk` is
> 30). Say if you meant the other reading.

**The M18 contrast clamp still holds, and E1 cannot have affected it.** `palettesim` sweeps **8,192
wallpaper/theme combinations** (full RGB cube, both themes): worst body **8.57:1**, worst caption
**5.64:1**, floor 4.5:1 — pass. The clamp's *input* is unchanged: `DeviceStatusReporter.themePayload()`
builds the wire payload from `dynamicLightColorScheme(context)` directly and **imports nothing from
`ui.theme`** (verified), so E1 changed what the phone *renders*, not what it *sends*.

## Acceptance

1. ✅ Desktop **0 errors, 2×CS9113** (= baseline). Android **155 tests, 0 failures** (= baseline).
   **Protocol constants still 18** both sides.
2. ✅ §0.3 verbatim above.
3. ✅ §A5 table above.
4. ✅ Tree — root: `.github/ .gitattributes .gitignore .gitmodules Assets/ CLAUDE.md CONTRIBUTING.md
   LICENSE Linc/ README.md Releases/ Vibe/` + gitignored `MCP/ .env/ .claude/ .continue/
   commit-msg.txt skills-lock.json`. `Linc/` = `ANDROID/ DESKTOP/ Documentation/ SCRCPY/ tools/`.
5. ✅ `git status --short | wc -l` = **766** pre-commit, **0** after. Working tree **2.2 GB** (mostly
   ignored build output); **staged content 153.5 MB / 1,270 files**, largest blob `libx265-216.dll`
   **22.5 MB** — under GitHub's 100 MB limit. `.git` 69 MB.
6. ✅ §B2 above — 131 files, three spot-checks.   7. ✅ §C5 — `updatesim` [7/7] against the real file (it walks up for it, so either CWD works).
8. **Harnesses, honestly.** 21 green (exit 0): `updatesim` **7/7**, `palettesim`, `storesim`,
   `outboxsim`, `homecachesim`, `homelayoutsim`, `synccachesim`, `filesim`, `syncsim`,
   `mirrorsettingssim`, `onboardsim`, `autostartsim`, `startupsim`, `appssim`, `applaunchsim`,
   `cleanroomsim`, `displaysim`, `hotspotsim` (after my fix), `presencesim`, `apkinstallsim`,
   `pccontrolsim`. **`packagesim` FAILS 1/11** — its M12i staleness guard, correctly: I edited source
   and did not re-publish (out of scope). **`desktopsim` fails only "No attached ADB device found";
   `blescan` fails "no Bluetooth radio". No phone was attached at any point**, so all hardware paths
   are unverified.
9. ✅ **Two negative proofs, both against production files, both restored.**
   **(a) `Linc/DESKTOP/Linc.Desktop/Services/UpdateDecision.cs`** — made `TryParse` lenient (junk chunk
   → `int.MaxValue`), so `min="@@@@"` outranks every build → `FAIL: the decision rule alone yields
   Forced for junk versions`, checks 1 and 6 fail, **exit 1**. Restored → exit 0. *My first break
   (junk → `0`) still passed, meaning check 6 was too loose to fail (GUIDE 4.4), so I **strengthened
   check 6** to assert `TryParse` rejects junk outright before re-proving. Saying so, not hiding it.*
   **(b) `Linc/DESKTOP/Linc.Desktop/Services/UpdateCheckService.cs`** — deleted the
   `if (!updatesEnabled())` gate → `FAIL: the handler saw 1 HTTP request(s) — expected 0` and the
   switched-off app returned `Forced`, **exit 1**. Restored → exit 0; with the gate,
   **`Requests == 0`, `LogLines == 0`** — zero network calls, proven as a number.
10. ✅ `settings.json` SHA256 **`90c4e5…6023` before and after — byte-identical** (`devicesim` not run).

## Committed, not pushed

```
9461f9a Open-source restructure: Linc/, Releases/, Assets/, Vibe/; MIT licence; repo-embedded update channel; Android dynamic colour restored
b761eaf M15a-M15c, M4c, M16: ...          d64a1b7 M14: black-and-white default theme, ...
```
`git status --short` → **empty**. Branch `main`. **No remote, no push, no force.** 1,280 files staged,
audited for `/obj/ MCP/ .env /x/ .gradle/ /build/ prod/ .apk .zip` — **0 of each**; 104 scrcpy binaries present.

**Sanity-check:** `.gitattributes`/`.gitmodules` moved to the root, submodule repointed
`SCRCPY/Default` → `Linc/SCRCPY/Default`. Left alone: `Linc/myeasylog.log`, `Linc/README.md`.

## Flagged for you

1. **`CLAUDE.md` is committed at the public root.** It documents the `nemo` MCP worker whose folder A4
   told me to exclude, and it auto-loads for any Claude Code session, contradicting
   `Vibe/START-HERE.md`. Not deleted (GUARDRAILS). **Recommend removing it before pushing.**
2. `DOCUMENTATION` vs `Documentation` — I followed the task file; say if the caps were deliberate.
   And 3. the VPN/CGNAT addresses in the A5 table — redact or not, your call.

## Manual test script (nothing below was verified — no phone attached)

1. Launch `Linc.Desktop`, open **Settings → Updates**. Expect one toggle **"Keep Linc up to date",
   ON**, a **Check now** button, and **no text box**.
2. Press **Check now** with it ON → expect *"Linc is up to date."* (the seeded manifest says so).
3. Turn it **OFF** → helper line names the Releases page; **Check now** → *"Turn on 'Keep Linc up to
   date' first."* and **no** network traffic.
4. Restart the desktop → toggle still OFF, and `settings.json` now carries `"UpdatesEnabled": false`
   with `"UpdateManifestUrl": null`. Turn it back ON.
5. Install the debug APK, open **Settings → Updates** on the phone: same one toggle, default ON, no
   text box, and the OFF text should name the Releases URL.
6. **Dynamic colour:** set a strongly coloured Android wallpaper, reopen Linc on the phone → it should
   now follow it (black-and-white before M19). With `UsePhoneColours` ON, confirm the PC tints too and
   all text stays readable (`palettesim` proves the maths; one look is still worth having).
