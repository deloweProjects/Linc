# M17a — report

## 0. First steps

**0.1** Branch `main`, HEAD `b761eaf`. **The tree was not clean.** It already carried Part A's regenerated icons and
most of Part B's Android rework, plus two untracked files (`ui/LincUi.kt`, `ui/theme/Dimens.kt`), with **no
`reports/M17a.md` on disk**. Icon mtimes all `2026-08-27 02:54`; `ico.png` is `02:17`. I did not commit, branch or
merge. Below is what each file **contains now** and what I verified myself; I make no claim about how the pre-existing
part got there.

**0.2 Baseline, matched exactly:** Desktop (VS MSBuild x64, Debug|x64) **0 errors, 2 warnings** (`CS9113` x2, both
`HomeViewModel.cs(166,72)`). Android **BUILD SUCCESSFUL, 140 tests, 0 failures**. **Protocol still 18**:
`protocol/Protocol.kt:12 const val PROTOCOL_VERSION = 18`, `Protocol/Envelope.cs:12 public const int Version = 18;`.
No protocol change made.

## 1. PART A — the mark

**A4 numbers, all measured by me from the files on disk:**
- **Source `ico.png`: 1269 x 1333, `Format32bppArgb`** — **not** M14's 713x737; the owner did replace it. A thin white
  L on a black plate; only **1.49%** of its opaque pixels are white — the artwork, not a defect.
- **Radius = 22% of the shorter side = 0.22 x 1269 = 279 px** (245 px inside the inset box). **Scale 0.88**, centred
  in a transparent margin.
- **Bytes:** `AppIcon.ico` 6,997 · `AppIconMono.ico` 7,383 · `Assets/linc.png` 409 · `SCRCPY/.../bin/linc.ico` 6,997 ·
  `SCRCPY/.../linc.ico` 6,997 · `ic_launcher_foreground_mark.png` 6,149. All three `.ico` are **byte-identical**
  (SHA256 `6D8DD3A7...C67CEC86`).
- `AppIcon.ico` carries **all 7 frames** (16/24/32/48/64/128/256), each PNG-compressed, each with **corner alpha = 0**
  and **~73.6% canvas coverage** — what 0.88 squared minus corner rounding predicts. `AppIconMono.ico`: same 7 frames,
  ~7.5% coverage, 100% white ink (tray variant).

**A4's 16 px tray check — by eye, not by statistic.** I extracted the 16 px frame, magnified it 16x nearest-neighbour
over magenta and looked at it: a **legible light-grey L on a rounded black plate with a transparent margin**. Not a
black blob.

**A2 Android:** `ic_launcher_foreground_mark.png` is 432x432 RGBA, a white L on **full transparency**, **no rounding
applied** — confirmed visually and by corner alpha, so **not double-rounded**, per A2. `ic_launcher_background.xml`
still flat `#000000`; `ic_launcher.xml` unchanged. **A3:** `ToolLocator`'s lookup order, candidate count and dev-tree
depth untouched.

## 2. PART B — the Android design system

`ui/theme/Dimens.kt` and `ui/LincUi.kt` hold the system M17-CODE specifies (4/8/12/16/24/32 scale, `radiusCard` 20 /
`radiusControl` 12, `touchTarget` 48, `trackpadMin` 280; `LincCard` tonal `surfaceContainer`, no elevation, no
outline; `LincScreen` top bar + scroll host; `LincEmpty`). `LincUi.kt` extends the skeleton with
`navigationIcon`/`actions` slots (Logs needs Back and Clear) and `LincSectionLabel`; `Theme.kt` wires `LincShapes`.

**What I changed this session**, on top of what the tree held:
- `HomeScreen.kt` — the **Media card was still an M3 `Card` with default elevation** and a
  `secondaryContainer`/`surfaceVariant` colour pair: the last mixed treatment B1 exists to remove. Now a `Surface` at
  `radiusCard`, no elevation. The playing/paused distinction is **kept**, moved onto the same ramp
  (`surfaceContainerHighest` vs `surfaceContainer`). Colour only — behaviour, gating and text untouched.
- Swept remaining **spacing** literals to `Dimens` (16→`l`, 12→`m`, 10→`m`, 8→`s`, 6→`s`, 4→`xs`). **Left as literals
  deliberately** (graphic dimensions, not spacing): progress bar `4.dp`, equaliser bars `2/3/12.dp`, avatar `56.dp`,
  canvas `24.dp`, icon `20.dp`, dot `10.dp`. **No `fontSize =` remains on any of the six screens.**

**Two deliberate deviations from M17-CODE (GUIDE 4.5):** (1) `ToolsScreen.kt` keeps the literal `heightIn(min =
280.dp)` rather than `Dimens.trackpadMin` — `ToolsScreenSourceTest` pins the literal string, so the constant would let
the number be edited without the guard noticing; a source comment says so. (2) `ToolsScreen.kt` was **not** ported
onto `LincScreen`; it keeps M16's own `Scaffold` + `TopAppBar` + scroll host, matched to `LincScreen`'s treatment.
Porting would rewrite the structure M16 landed and the source test guards, for no visual gain.

**B3:** `ToolsScreenSourceTest` is **green and unmodified — no check flipped or weakened.** Media, Presentation and
Quick-controls keep behaviour, "disable never hide" gating and blocked-reason text.

### B4 — per-screen before → after (source-derived)
**Derived from the source diff, NOT from screenshots** — no phone was reachable (section 3), so **no captures were
taken and none of this was observed rendering.**

- **Home** — bare `Scaffold`, no title → `LincScreen("Home")`; Media card loses M3 elevation and its second colour
  system; `LincSectionLabel("Share")` groups the share block; two bare empty states → `LincEmpty` (icon + sentence).
- **Tools** — M16 scroll host + 280 dp floor retained; treatments unified to `radiusCard`/`surfaceContainer`; row
  spacing → `Dimens.s`; touch targets floored at 48 dp.
- **Settings** — → `LincScreen("Settings")`; loose rows regrouped into four titled `LincCard`s (Permissions / Hotspot
  / Mirror / About).
- **Status** — bare `Scaffold` + raw `Card` → `LincScreen("Status")` + two `LincCard`s, the second titled
  `status_phone_header`; captions → `onSurfaceVariant`.
- **Logs** — → `LincScreen("Logs")`, Back leading / Clear trailing; empty case → `LincEmpty`; the log list is now one
  `radiusCard` surface holding a `LazyColumn` instead of free-floating rows (`scrollable = false`, it scrolls
  internally). **Onboarding** — → `LincScreen(step.title)` + `LincCard`.

## 3. PART C — NOT DONE. The Pixel 7 was not reachable.

**The override authorised phone work, but the phone was not attached.** Not caution — I could not reach it. `adb
devices -l` on **both** 5037 and 5038: **empty** (neither daemon was even running; I started both, queried, stopped
both). `adb mdns services`: nothing — no wireless endpoint. Windows PnP sees the Pixel 7 **only as Bluetooth audio**
(`A2DP SNK`, `Hands-Free HF`, `Avrcp`): **no USB ADB interface present**. **Independent corroboration:** `desktopsim`
fails on exactly one check — `No attached ADB device found`. The task predicted it would now pass; **it does not.** I
did not guess an IP and `adb connect` — C3 says not to improvise around hardware I cannot see.

- **C1 install — SKIPPED, no device.** APK built: `app-debug.apk`, **25,968,889 bytes**, `2026-08-27 22:21`. **Not
  installed.**
- **C2 screen walk (portrait + landscape) — SKIPPED. C3 M16 step 4 (play from phone resumes PC) — SKIPPED, still never
  observed. C3 M4c — SKIPPED. C3 M15c step 8 — SKIPPED**, no USB link to pull.
- **C4 defects — none, because nothing was observed.** **B4 captures — NOT TAKEN.**

**Nothing was installed, tapped, launched or captured on any device. No phone state to restore.**

## 4. Acceptance

1. Both sides build; Android **140 / 0**; desktop 0 errors / 2x `CS9113`; **protocol 18** (0.2).
2. A4's numbers in section 1, **including the 16 px tray check, done by eye**.
3. B4's list is in section 2 but is **source-derived, not observed**.
4. **C1-C3 all SKIPPED, device not reachable**, each with its reason (section 3).
5. Harnesses, re-published first (`-t:Publish -p:PublishProfile=Beta`, Release|x64, succeeded): `packagesim`
   `cleanroomsim` `pccontrolsim` `hotspotsim` `homelayoutsim` `presencesim` `startupsim` `applaunchsim`
   `mirrorsettingssim` `appssim` `displaysim` `homecachesim` `storesim` `synccachesim` `outboxsim` `apkinstallsim`
   `autostartsim` `syncsim` — **all 18 exit 0, PASS**. **`desktopsim` FAILS** on one check: `No attached ADB device
   found`. It did **not** start passing.
6. Two negative proofs, both against **production files**, both restored: **(a) scroll host** — broke
   `ANDROID/.../ui/ToolsScreen.kt` (replaced `.verticalScroll(rememberScrollState())` with a padding modifier) →
   `ToolsScreenSourceTest > the Tools page has a scroll host FAILED`, `140 tests completed, 1 failed` → restored →
   `BUILD SUCCESSFUL`, 140/0. **(b) icon** — see section 5.
7. `settings.json` SHA256 **identical before and after**:
   `0FD70C1AAC5864583EE7C35B9EF501DC9D1D4712861DB9F979F574C92BBFCEC9`. All three real serials, including
   `<your-device-serial>`, intact.
8. N/A — the phone was never touched, so there was no state to return to Home.

## 5. Task-file defect — acceptance 6(b) was not satisfiable as written

**6(b) says reverting the icon's alpha/rounding makes `packagesim` check 5 fail. It does not.** Check 5 is a
`File.Exists`; it passes for *any* file named `linc.ico`. I proved this rather than asserting it: I swapped in a
**207-byte flat opaque black square, one frame, no alpha, no rounding**, ran `packagesim`, and got **`PASS: All
verification checks succeeded.`** The geometry had no guard at all.

**So I built the guard** (GUIDE 4.1). Three sub-checks added to `tools/packagesim/Program.cs` assert the shipped
icon's *actual* geometry: the 7-size frame set, corner alpha 0 on the 16 px frame, and 50-95% opaque coverage.
Decoding is hand-rolled ICO-directory + PNG using BCL `ZLibStream` only — **no new dependency**. It independently
reproduces my measurements (`alpha=0`, `73.8% opaque`). **Negative proof (b):** flattened `linc.ico` to a 16 px opaque
black square → **3 FAILs** (`missing frame sizes`, `rounding/alpha reverted`, `inset/scale reverted`) → restored →
`PASS: All verification checks succeeded.`, SHA256 back to `6D8DD3A7...C67CEC86`.

**Also:** 0.1 expected a clean tree and it was not; the `desktopsim` prediction rested on the phone being attached;
`ico.png` is 1269x1333, not the 713x737 M14 recorded.

## 6. Manual test script — everything section 3 could not do
1. `adb devices` — confirm `<your-device-serial>` appears. If empty, enable USB debugging and accept the RSA prompt on the
   phone.
2. From `Linc/ANDROID`: `gradlew assembleDebug`, then `adb install -r app\build\outputs\apk\debug\app-debug.apk`.
3. Launch Linc. On **Home, Tools, Settings, Status, Logs**: confirm each has a title bar naming the screen, every card
   has the same rounded tonal treatment (no shadows, no outlines, no second colour system), and each page **scrolls**
   to its bottom.
4. Rotate to **landscape**, repeat step 3, watching for clipped cards or overlapping text.
5. **Launcher icon:** the L in a rounded black tile with a small margin, not edge to edge. **Windows tray icon** at
   100% and 125% scaling — legible L, not a black blob.
6. **M16 step 4 (never yet observed):** start a media player on the PC, pause it there, then press play **on the
   phone's Home Media card**. The PC must resume.
7. **M4c:** confirm the Media card names the track and prev/play/next work; open Notepad on the PC, tap the
   Presentation card's keys, confirm **one keypress per tap**.
8. **M15c step 8:** on USB, pull the cable; confirm Linc reports the drop in plain language and recovers on replug.
9. Report any clipping, overlap or unreadable contrast with the screen name and orientation.
