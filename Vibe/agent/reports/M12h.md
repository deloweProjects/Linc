# M12h report

**§0.1 — what's actually on disk (before any edit):**
- `SettingsViewModel.cs:189-205` — `StartWithWindows` getter reads `_registry.StartWithWindows`; setter calls `_registry.SaveStartWithWindows(value)` then `_startup.Enable(...)`/`.Disable()`, then `OnPropertyChanged()`. Matches the "ruled out" description exactly.
- `StartupRegistration.cs` — `Enable()`/`Disable()` wrap `Registry.CurrentUser` calls in try/catch, logging only on the `catch` (`Error`) branch — the observability gap the task names.
- `SettingsPage.xaml:144` — `IsOn="{x:Bind ViewModel.StartWithWindows, Mode=TwoWay}"`. Untouched (not `MainWindow.xaml*`).
- `DeviceRegistry.cs`: `Settings` record (line 186-206) has `StartWithWindows` appended last, as documented. **Checked the planner's specific hypothesis by hand: `Persist()` (line 559-570) constructs `new Settings(...)` positionally, and `StartWithWindows` is the 15th/last positional argument — it lines up exactly with the record's 15th parameter.** The load path (line 334) reads it back with `?? false`. The hypothesized "appended to record but omitted from `Persist()`" defect **does not exist in this code.**

**§0.2 — baseline build:** first run was a no-op (process already built/running, 3-line output, 0/0). Redid it as a forced `-t:Rebuild` for an honest number: **0 errors, 1 warning** (`CS9113` in `HomeViewModel.cs:166`, `PhotoVm`'s unused `connection` parameter — confirmed via `git diff` to be committed/pre-existing, untouched by this session).

**Branch: none of A/B/C reproduce.** Evidence, all against the real running exe via UI-Automation `TogglePattern`/`SelectionItemPattern` (no mouse movement, per the guide):
- Toggled ON → log recorded `DIAG-M12h: StartWithWindows setter entered, value=True` (setter runs — not Branch A) then `StartupRegistration: Enable succeeded. Wrote "Linc"="...` (Enable doesn't throw — not Branch B).
- Registry after ON: `Linc : "Linc/DESKTOP\Linc.Desktop\bin\x64\Debug\net8.0-windows10.0.19041.0\Linc.Desktop.exe" --startup` — present, quoted, `--startup` suffix correct.
- Toggled OFF → registry read: **absent** (`Get-ItemProperty` returned nothing for `Linc`).
- **Restart survival:** toggled ON, `Stop-Process -Force`, relaunched, navigated to Settings again — toggle read **On**, registry key still present, unchanged.

So the mechanism (setter → `SaveStartWithWindows` → `Persist()` round-trip → `Enable`/`Disable` → registry) works correctly end-to-end on the code as it sits on disk today. Worth flagging per Branch C's callout: the Run value points at the Debug build path, not an installed location — expected for this dev build, not a defect.

**Fix applied (regardless of branch, per §3):** `StartupRegistration.Enable()`/`Disable()` now log `Info` on success naming the value/path written or removed — closes the exact observability gap that made this hard to diagnose. Removed the temporary `DIAG-M12h` setter log before finishing (`SettingsViewModel.cs` has **zero net diff** from what M12e left).

Since Branch A didn't hold, I did **not** add the conditional autostartsim round-trip check verbatim as specified — instead I gave `autostartsim` a `SpyLogService` and two new assertions that Enable()/Disable() actually log success, since that's the thing I actually changed. Judgment call, flagging it explicitly.

**Negative proof:** commented out the new success-log line in `Enable()` (production file `DESKTOP/Linc.Desktop/Services/StartupRegistration.cs`) → `autostartsim` printed `FAIL Enable() logs an Info line on success` / `1 FAILURE(S)`. Restored the line → `ALL CHECKS PASSED`.

**Harnesses:** `autostartsim` green (12 checks incl. 2 new). Full regression list green: `packagesim, presencesim, startupsim, applaunchsim, mirrorsettingssim, appssim, homelayoutsim, displaysim, homecachesim, storesim, synccachesim, outboxsim, apkinstallsim, syncsim`. `cleanroomsim` failed once on a 5s log-settle timing race in the `--data-root`/`LogService` path (untouched by this session) — passed clean on two immediate reruns; treating as a pre-existing flake, not mine to fix (out of scope). `desktopsim`: fails on "No attached ADB device found" (no phone attached — hardware, expected). `blescan`: passed this run (sighted the pinned Pixel 7 twice); its rotating-id failure is pre-existing/hardware-dependent per the task and wasn't chased either way.

**settings.json:** before session 1542 bytes / `2026-08-08T18:55:01`; after (toggled ON→OFF→ON→OFF during live verification, ending back at `StartWithWindows:false`, matching the original) 1542 bytes / `2026-08-08T19:47:39`. Same size (value round-tripped to its original `false`), mtime changed only by my toggling for the required registry-read checks.

**Files changed:**
- `DESKTOP/Linc.Desktop/Services/StartupRegistration.cs` — added `Info`-level success logging to `Enable()`/`Disable()`.
- `tools/autostartsim/Fakes.cs` — added `SpyLogService : ILogService`.
- `tools/autostartsim/Program.cs` — round-trip test now injects the spy and asserts success logging.
- `SettingsViewModel.cs`, `SettingsPage.xaml`, `DeviceRegistry.cs`, `App.xaml.cs`, `MainWindow.xaml*` — read, not modified.

**Manual test for the owner:** Open Linc → Settings → toggle "Start Linc when I sign in" ON → confirm it appears in Windows Settings → Startup apps → sign out and back in.

**Could not verify:** an actual sign-out/sign-in cycle (task forbids signing the owner out) — everything short of that (registry write, restart survival) is proven above.

**Ambiguous/contradictory in the task file:** the task's whole premise (owner sees nothing happen, M12g found no Run value) doesn't reproduce against the current on-disk code — I can't tell whether M12g tested a stale/unrebuilt exe or something already got fixed since, but today's code, tested against the real production path three separate ways, works. Also, git status confirms this whole feature (`SaveStartWithWindows`, `StartupRegistration`, the M9b notification-history fields) is still **entirely uncommitted** in the tree, consistent with "nothing about M12e was ever reviewed."
